Drop optional 512x512 PNG portraits here, named exactly:

  sage.png
  philosopher.png
  healer.png
  elder.png
  storyteller.png

If a PNG exists, the SDK will use it for the central hero portrait
automatically. If not, a themed inline SVG silhouette is used instead.
